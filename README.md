# Agent Constellation POC (JSON-based)

- Multi-agent architecture (Orchestrator + Worker Agents + Evaluator + Composer)
- JSON used as storage for tasks, memory, config, and agents.
- Single command POC you can run locally and host on GitHub.

## Run

```bash
npm install
npm run dev
